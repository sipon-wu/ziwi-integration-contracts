# mfg 预发布对齐移交物（2026-07-12）

> **性质**：移交物（见《流程闭环规范 V1》〇.2「移交必须带移交物」）。
> **归属决策**：mfg 归 **workbuddy 团队闭环**。本件由 codebuddy 侧完成预发布对齐后移交，workbuddy mfg 团队凭此证据认领、按共享仓 SOP 自行闭环后续待办。
> **状态流转建议**：`Draft(已完成对齐)` → workbuddy 认领 → `In-Progress(N5)` → `QA-Passed` → `Released`。请在根目录 `STATUS.md` 增加一行显形（见文末）。
> **2026-07-12 更新（方案 A 已完成）**：workbuddy 已获 CVM root SSH key，且 `deploy.sh` 已推 github main（`c21bc65`）。workbuddy 可在 CVM 上 `git pull && ./deploy.sh` 自闭环，**codebuddy 侧不再代执行 mfg 部署**。服务器通用策略/坑见 `runbooks/CVM部署通用规范与坑清单.md`。

---

## 一、已完成（带证据，codebuddy 侧已做）

| 项 | 结论 | 证据 |
|----|------|------|
| 源码对齐到权威 | `backend/` 已 `git pull` 到 `origin/main` **`336899f`**（含 N3 修复） | `git rev-parse HEAD` = 336899f |
| 部署副本对齐 | `deploy/backend/`（untracked 手动 rsync 副本）已 `rsync -a --exclude=Dockerfile --exclude=.git backend/ deploy/backend/`，与源码一致 | 保留 deploy 的阿里云 pip 镜像 Dockerfile |
| 容器重建 | `mfg1-backend` 重建，镜像 `deploy-mfg-backend:latest`，digest `454fde09…`，状态 `Up (healthy)` | `docker ps` + `docker inspect` 镜像一致 |
| DB 安全 | `mfg1-db` **未动**，数据卷安全（仅 `--no-deps` 重建 backend，旧容器先 `docker rm -f`） | db 仍在跑、未重建 |
| N3 修复生效 | 真实路径 `/api/v1/wms/locations` 返回 **401**（端点存在、需鉴权） | 容器内 curl 实测 |
| 真实 data.id | WMS 创建响应返回真实 `data.id`（随 `336899f` 上线） | commit 336899f 已含 |

**重要纠正（避免重复踩）**：之前探针报「`GET /wms/locations` 404 无列表接口」是 **探针自身路径错（缺 `/api/v1` 前缀）**，非后端缺失。真实端点在 `app/api/wms.py` `@router.get("/locations")`，路由前缀 `/api/v1/wms`。

---

## 二、待办（移交 workbuddy 闭环，非 codebuddy 续做）

### P0 — N5 过账 / 流水仍是真空心（真实能力缺口）
- **现象**：创建收/发/出订单**只建档、不过账、不写 transactions**；N5 对账探针（收+100/发-10/出-5 → 库存应=85、流水应 3 条）全 FAIL。
- **不是路径错，是 BUG-5 之外未实现的能力缺口**。
- **需先拍板过账语义**（业务口径，workbuddy PM 整理选项）：
  1. 创建即过账（写入即记账），或
  2. 独立 `confirm`/`post` 动作显式过账。
- **再走**：架构（同事务一致性，库存+流水原子写）→ 工程接线 → 真 token 实测 + N5 对账探针通过后，才标记 `QA-Passed`。

### P1 — 探针路径修正
- 修 N5 探针：`/wms/*` → `/api/v1/wms/*`，重跑对账，避免再产出虚假 404。

### P2 — 部署链路固化（根治分离坑）
- 把「`rm 游离迁移 → git pull → rsync backend→deploy/backend（保留 Dockerfile）→ docker compose up -d --build --no-deps mfg-backend`」固化成 `mfg/deploy.sh`，杜绝「git pull 只动 backend/、容器跑 deploy/backend 旧副本」的反复。
- 注：CVM 上的游离迁移 `backend/migrations/add_work_order_status_logs_tenant_id.sql` 已删除，pull 已通过。

---

## 三、workbuddy 必须读的 SOP（现成答案，勿重踩）

1. **`runbooks/school双环境部署工作流.md`**
   - 必踩坑清单第 1 条：后端改了代码，staging 没生效 → 先 rsync 源码到服务器再 up --build（mfg 本次即此坑同构）。
   - GATE 交付检查清单（staging 验证 / rsync / env-file / QA 全量用例 / 不擅自清数据）。
   - mfg 的「部署副本分离」坑，跨项目通用版见 `runbooks/CVM部署通用规范与坑清单.md` 第三节 / 第四节 G1。
2. **`测试标准/AI测试Agent约束指南.md`** 第七、八节（平台专项检查单 + 历史缺陷模式库）→ 对接 `QA_从严回测用例集_V1`。
3. **`流程闭环/状态迁移与移交规范_V1.md`** Stage-Gate / Bug 生命周期 / 部署变更生命周期；进入 `Ready-for-QA` 前须逐条标注平台专项覆盖。
4. **完成门槛（任何 mfg 交付强制带）**：本地 build 通过 → 真 token 实测端点 → 全回归 → **部署对齐脚本跑通** → **业务对账探针通过**。缺一项不算 done。

---

## 四、移交流转

- **进入判据**（workbuddy 认领）：本移交物证据齐全，N3 已验证生效，可认领。
- **退出判据**：N5 过账/流水按上面口径实现，真 token 实测 + 对账探针全绿，且 `deploy.sh` 固化，状态进 `STATUS.md` 并置 `Released`。
- **责任人**：workbuddy mfg 团队（开发 + QA）；过账语义由 workbuddy PM 拍板。
- **超时升级**：停在非终态超 2 工作日，按规范升级（开发→技术负责人→项目经理）。

---

## 五、STATUS.md 建议行（请 workbuddy 维护根目录 STATUS.md）

```
| 类型 | 编号 | 标题 | 当前状态 | 责任人 | 超时日 |
| 移交 | MFG-20260712 | 预发布对齐完成，N5 过账/流水空心待闭环 | In-Progress(N5) | workbuddy mfg | 2026-07-14 |
```
